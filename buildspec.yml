version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
  build:
    commands:
      - zip -r deployment.zip . -x "*.git*"
  post_build:
    commands:
      - echo "Build completed. Zipped code ready."

artifacts:
  files:
    - deployment.zip
